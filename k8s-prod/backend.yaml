---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: prod
  labels:
    app: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: adijaiswal/backend:694535bf15cb7328bd153687abca4167a3fd083a
          ports:
            - containerPort: 5000
          env:
            - name: DB_HOST
              value: mysql
            - name: DB_USER
              value: root
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_ROOT_PASSWORD
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: mysql-config
                  key: MYSQL_DATABASE
---
apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: prod
  labels:
    app: backend
    monitoring: "true"          # <— ServiceMonitor selector
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 5000
      targetPort: http
    - name: metrics              # <— what ServiceMonitor scrapes
      port: 9090
      targetPort: metrics
  type: ClusterIP
